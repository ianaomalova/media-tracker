import { Text } from 'react-native';
import Screen from '@/components/Screen';

export default function HomeScreen() {
  return (
    <Screen edges={['top']}>
      <Text>HOME78</Text>
    </Screen>
  );
}
